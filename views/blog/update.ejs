<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs') %>
<body>
    
    <%- include('../partials/nav.ejs') %>

    <h2 style="text-align: center">Update Blog</h2>

    <div class="create-blog content">
        <form action="/blogs/update/<%= blog._id %> " method="POST">
            <!-- the name attribute must be added so that we
                can access the form data in the POST request  -->
                <div class="cancel-postion">
                    <a class="cancel" href="/blogs/<%= blog._id %>">Cancel Editing</a>
                </div>
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" value="<%= blog.title %>" required>
                <label for="body">Content:</label>
                <textarea id="body" name="body" required><%= blog.body %></textarea>
                <button type="submit">Update</button>
        </form>
    
    </div>

    <%- include('../partials/footer.ejs') %>

</body>
</html>